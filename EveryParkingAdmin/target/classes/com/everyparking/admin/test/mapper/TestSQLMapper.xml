<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.everyparking.admin.test.mapper.TestSQLMapper">
	<insert id="DBTest">
		insert into test values (2)
	</insert>
	
	<select id="getDBTest" resultType="java.util.LinkedHashMap">
		select * from test
	</select>

	<insert id="DBUploadTest">
		insert into uplaodTest values (nextval(sq_test), #{editorData}, sysdate())
	</insert>
	
	<select id="getUploadFile" resultType="map">
		select * from uplaodTest where test_no = 8
	</select>
</mapper>